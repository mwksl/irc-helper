# Write some tests, Trevor
import unittest

class TestFindNames(unittest.TestCase):
    def test_find_by_ip:
        #This isn't the correct format that the method should return
        sef.assertEqual(getAllIPsUsedByName('treevs'), '127.0.0.1')
